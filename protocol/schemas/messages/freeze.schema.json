{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "freeze.schema.json",
  "title": "FreezeMessage",
  "description": "Toggle freeze mode. Holds the current visualization frame when enabled. Also accepted as 'set_freeze'.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "type": {
      "enum": ["freeze", "set_freeze"]
    },
    "v": {
      "type": "string"
    },
    "enabled": {
      "type": "boolean",
      "description": "Whether to enable freeze. If omitted, toggles current state."
    }
  },
  "required": ["type"]
}
