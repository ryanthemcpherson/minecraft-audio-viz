/**
 * Phase 0 security quarantine tombstone for the legacy self-updater.
 *
 * This module is intentionally unreferenced and has no runtime behavior. Keep
 * the tombstone until a separately reviewed release mechanism replaces it.
 */
export function useAutoUpdate(): null {
  return null;
}
